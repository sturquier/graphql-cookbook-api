type Query {
  recipes(limit: Int, offset: Int, sortBy: String): [Recipe]
  recipe(id: Int): Recipe
}

type Mutation {
  createRecipe(title: String, description: String, categoryIds: [Int]): Recipe
  deleteRecipe(id: Int): Boolean
}
